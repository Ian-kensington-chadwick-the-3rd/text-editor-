{
  "name": "jate",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start:dev": "concurrently \"cd client && npm run build\" \"cd server && node server.js\" ",
    "start": "npm run build && cd server && node server.js",
    "server": "cd server && node server.js",
    "build": "cd client && npm run build",
    "install": "cd server && npm i && cd ../client && npm i",
    "client": "cd client && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
